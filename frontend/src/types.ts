export interface GameGuess {
  name: string;
  releaseYear: number;
  genre: string;
  developer: string;
  publisher: string;
  platform: string;
}

export interface NavigationItem {
  icon: string;
  ariaLabel: string;
}
